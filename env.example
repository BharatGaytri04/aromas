# Django Settings
# Generate a secret key: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-secret-key-here
DEBUG=False

# Allowed Hosts (comma-separated)
# For production: include your domain and server IP
ALLOWED_HOSTS=aromasbyharnoor.com,www.aromasbyharnoor.com,193.203.162.238

# CSRF Trusted Origins (comma-separated, include https:// protocol)
# Example: https://aromasbyharnoor.com,https://www.aromasbyharnoor.com
CSRF_TRUSTED_ORIGINS=https://aromasbyharnoor.com,https://www.aromasbyharnoor.com

# Admin URL (custom admin path for security)
ADMIN_URL=secure-admin/

# Database Configuration (PostgreSQL for production)
DB_NAME=aromas_db
DB_USER=aromas_user
DB_PASSWORD=change-this-to-strong-password
DB_HOST=localhost
DB_PORT=5432

# Email Configuration
# For Gmail: Use App Password (not regular password)
# Get App Password: https://myaccount.google.com/apppasswords
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=aromasbyharnoor@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password-here
DEFAULT_FROM_EMAIL=aromasbyharnoor@gmail.com

# Razorpay Payment Gateway (Optional)
RAZORPAY_ENABLED=False
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_CURRENCY=INR
